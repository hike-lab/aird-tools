# 데이터 정제 계획

## 대상 파일
- `data/public_data_catalog_20260228.csv`
- 총 95,544건, 36개 컬럼

## 총괄 현황

| 항목 | 수치 |
|------|------|
| 총 레코드 | 95,544건 |
| 컬럼 수 불일치 행 | 0건 (CSV 구조 정상) |
| 결측 표현 | `"-"` 89만건, `""` 407건 |

---

## 정제 항목

### 1. 결측값 통일

- **영향**: 전체
- **현황**: `"-"`, `""` 두 가지 패턴 혼재
- **정제**: `"-"`, `""` → null 처리, 해당 트리플 미생성
- **컬럼별 결측률 (상위)**:
  - 시간범위 95.6%, 데이터 한계 95.1%, 공간범위 94.3%
  - 수집방법 91.8%, 보유근거 88.9%
  - API 유형/트래픽/심의 87.4% (FILE/STD에는 원래 해당 없음)

### 2. 확장자(데이터포맷) 대소문자 정규화 + 오타 수정

- **영향**: 95,544건
- **현황**: 32쌍의 대소문자 중복
  - `csv`/`CSV` (64,107 + 2,796)
  - `pdf`/`PDF`/`pdF` (2,610 + 746 + 1)
  - `json`/`JSON` (52 + 1,744)
  - `xml`/`XML` (240 + 4,504)
- **오타**: `xlxs`(3건) → `xlsx`, `cvs`(2건) → `csv`
- **정제**: 소문자 정규화 + 오타 수정

### 3. 설명 필드 HTML 태그 제거

- **영향**: 47,761건 (50%)
- **현황**: `<br/>`, `<br>`, `<p>` 등 혼재
- **정제**: HTML 태그 strip → 순수 텍스트

### 4. 전화번호 백틱(`) 접두어 제거

- **영향**: 95,137건 (결측 407건 제외)
- **현황**: 모든 전화번호가 `` ` ``으로 시작 (예: `` `0442006788 ``)
- **정제**: 백틱 제거, `tel:` URI 변환 시 숫자만 유지

### 5. 이용허락범위 유형코드 정규화

- **영향**: 95,244건
- **현황**: 장문의 비정규 문자열
  ```
  "제3자 권리 포함 : 저작권 표시/공공저작물 : 출처표시 (제 1유형)"
  "공공저작물 : 출처표시, 상업적 이용금지, 변경금지 (제 4유형)"
  ```
- **정제**: 유형번호 추출 정규화

| 원본 패턴 | 정규화 코드 | 건수 |
|----------|-----------|------|
| 이용허락범위 제한 없음 | `KOGL-TYPE-1` | 80,387 |
| 자유이용 (제 0유형) | `KOGL-TYPE-0` | 148 |
| 출처표시 (제 1유형) | `KOGL-TYPE-1` | 5,148 |
| 출처표시, 상업적 이용금지 (제 2유형) | `KOGL-TYPE-2` | 367 |
| 출처표시, 변경금지 (제 3유형) | `KOGL-TYPE-3` | 602 |
| 출처표시, 상업적 이용금지, 변경금지 (제 4유형) | `KOGL-TYPE-4` | 2,273 |
| 제3자 권리 포함 + 유형 | `THIRD-PARTY` + 해당 유형 | 6,206 |
| `-` (결측) | null | 300 |

### 6. 비용부과유무 boolean 변환

- **영향**: 95,544건
- **현황**: `"무료"` 95,202 / `"유료"` 42 / `"-"` 300
- **정제**: `무료` → `true`, `유료` → `false`, `-` → null

### 7. 시간범위 날짜 파싱

- **영향**: 4,184건 (비결측)
- **현황**: 형식 비정규
  ```
  "2025년"
  "2024년 1월 ~ 2024년 12월"
  "2024년 1월 - 2024년 12월"
  "2004년~2024년"
  "2025년1월~2025년12월"
  "현재"
  ```
- **정제**: 정규표현식으로 시작/종료 날짜 추출 → `schema:startDate`/`schema:endDate`

### 8. 신청가능 트래픽 숫자 추출

- **영향**: 12,034건 (API만 해당)
- **현황**:
  ```
  "개발계정 : 10000/ 운영계정 : 활용사례 등록시..."  →  devQuota: 10000
  "개발계정 : 1000/ ..."                          →  devQuota: 1000
  "해당 기관의 정책에 따라 트래픽 수는 상이 할 수 있음"  →  텍스트 유지
  ```
- **정제**: 숫자 추출 가능한 경우 정수 분리, 아닌 경우 원문 유지

### 9. 공간범위 정규화

- **영향**: 5,464건 (비결측)
- **현황**: 표현 비정규
  ```
  "대한민국" / "대한민국(전국)" / "전국"
  "대한민국 광주" / "대한민국 충청남도 공주시"
  "경상북도 경산시, 고령군, 칠곡군"
  ```
- **정제**: 행정구역 코드 연계 필요 (외부 코드체계 매핑)
- **비고**: 현재는 label 유지, 향후 행정표준코드 연계 시 URI화

---

## 우선순위

| 순위 | 항목 | 영향 범위 | 난이도 | 비고 |
|------|------|----------|--------|------|
| 1 | 결측값 통일 | 전체 | 낮음 | 변환 필수 전제 |
| 2 | 확장자 대소문자 정규화 + 오타 | 95,544건 | 낮음 | |
| 3 | HTML 태그 제거 | 47,761건 | 낮음 | |
| 4 | 전화번호 백틱 제거 | 95,137건 | 낮음 | |
| 5 | 이용허락범위 유형코드 정규화 | 95,244건 | 중간 | |
| 6 | 비용부과유무 boolean 변환 | 95,544건 | 낮음 | |
| 7 | 시간범위 날짜 파싱 | 4,184건 | 중간 | |
| 8 | 신청가능 트래픽 숫자 추출 | 12,034건 | 중간 | |
| 9 | 공간범위 정규화 | 5,464건 | 높음 | 외부 코드체계 필요 |
